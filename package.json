{
  "name": "parallel-frame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal review journal — games, visual novels, film.",
  "scripts": {
    "dev": "eleventy --serve --port=4173",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
